---
- name: Start systemd service - systemd-timesyncd
  ansible.builtin.systemd:
    name: systemd-timesyncd
    state: started
    enabled: yes
  become: true

- name: Install packages
  community.general.pacman:
    name:
      - reflector
      - pacman-contrib
      - xdg-user-dirs
    state: present
  become: true

- name: Update xdg-user-dirs
  ansible.builtin.shell: xdg-user-dirs-update  # noqa command-instead-of-shell
  changed_when: no

- name: Start systemd service - paccache.timer
  ansible.builtin.systemd:
    name: paccache.timer
    state: started
    enabled: yes
  become: true

- name: Update reflector configuration file
  ansible.builtin.template:
    src: reflector.conf.j2
    dest: /etc/xdg/reflector/reflector.conf
    backup: no
    owner: root
    group: root
    mode: 0644
  become: true

- name: Start systemd service - reflector.service
  ansible.builtin.systemd:
    name: reflector.service
    state: started
  become: true

- name: Start systemd service - reflector.timer
  ansible.builtin.systemd:
    name: reflector.timer
    state: started
    enabled: yes
  become: true

- name: Update pacman configuration file
  ansible.builtin.template:
    src: pacman.conf.j2
    dest: /etc/pacman.conf
    backup: no
    owner: root
    group: root
    mode: 0644
  become: true

- name: Upgrade system
  community.general.pacman:
    update_cache: yes
    upgrade: yes
  become: true
